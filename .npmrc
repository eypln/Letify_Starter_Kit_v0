package-manager=pnpm
engine-strict=true

# Suppress esbuild install script warnings (tsx dependency)
# These are optional platform-specific optimizations that don't affect functionality
ignore-scripts=true
